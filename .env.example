# Run browser in non-headless mode so you can see it
HEADLESS=false

# Slow actions down by 250ms each (useful for debugging)
SLOW_MO=250

# Add extra Chromium flags:
#  - disable GPU acceleration
#  - set window size
#  - ignore HTTPS certificate errors
BROWSER_ARGS=--disable-gpu,--window-size=1280,800,--ignore-certificate-errors
